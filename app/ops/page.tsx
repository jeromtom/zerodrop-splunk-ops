import { OpsDashboard } from "@/components/OpsDashboard";

export const dynamic = "force-dynamic";
export const metadata = { title: "DropWatch — Agentic Ops" };

export default function OpsPage() {
  return <OpsDashboard />;
}
